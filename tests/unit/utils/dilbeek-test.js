import dilbeek from 'rdflib-now/utils/dilbeek';
import { module, test } from 'qunit';

module('Unit | Utility | dilbeek', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = dilbeek();
    assert.ok(result);
  });
});

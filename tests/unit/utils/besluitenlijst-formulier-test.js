import besluitenlijstFormulier from 'rdflib-now/utils/besluitenlijst-formulier';
import { module, test } from 'qunit';

module('Unit | Utility | besluitenlijst-formulier', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = besluitenlijstFormulier();
    assert.ok(result);
  });
});

import graphs from 'rdflib-now/utils/graphs';
import { module, test } from 'qunit';

module('Unit | Utility | graphs', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = graphs();
    assert.ok(result);
  });
});

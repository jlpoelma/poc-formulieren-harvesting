import importTriplesForForm from 'rdflib-now/utils/import-triples-for-form';
import { module, test } from 'qunit';

module('Unit | Utility | import-triples-for-form', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = importTriplesForForm();
    assert.ok(result);
  });
});

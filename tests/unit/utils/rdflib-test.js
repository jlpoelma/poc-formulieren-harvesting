import rdflib from 'rdflib-now/utils/rdflib';
import { module, test } from 'qunit';

module('Unit | Utility | rdflib', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = rdflib();
    assert.ok(result);
  });
});
